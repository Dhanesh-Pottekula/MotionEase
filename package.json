{
  "name": "head-tracker",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@craco/craco": "^7.1.0",
    "@tensorflow-models/pose-detection": "^2.1.3",
    "@tensorflow/tfjs": "^4.22.0",
    "concurrently": "^9.1.0",
    "cra-template": "1.2.0",
    "path-browserify": "^1.0.1",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1",
    "wait-on": "^8.0.1",
    "web-vitals": "^4.2.4"
  },
"scripts": {
  "start": "react-scripts start",
  "react-start": "craco start",
  "electron": "wait-on http://localhost:3000 && electron ./electron/main.js",
  "build": "craco build && electron-builder",
  "test": "craco test",
  "eject": "craco eject"
},
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "electron": "^33.2.1",
    "electron-builder": "^25.1.8",
    "electron-is-dev": "^3.0.1"
  }
}
